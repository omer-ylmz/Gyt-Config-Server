eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:9000/eureka
  instance:
    instance-id: ${spring.application.name}:${instanceId:${random.value}}
    preferIpAddress: true
server:
  port: 9003
spring:
  application:
    name: examservice
  datasource:
    url: jdbc:postgresql://host.docker.internal:5435/examdev
    username: postgres
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      javax:
        persistence:
          validation:
            mode: none
    show-sql: true
client:
    questionservice:
      address: "localhost:9002"
      keep-alive-without-calls: true
      enable-keep-alive: true
      negotiationType: "PLAINTEXT"
